package {{name}}
  extends Modelica.Icons.Package;
  annotation (
    uses(
      Modelica(
        version="{{modelica_version}}"),
      Buildings(
        version="{{mbl_version}}")),
    version="1");
end {{name}};
